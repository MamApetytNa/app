import React from 'react';

import { storiesOf } from '@storybook/react';

import Messenger from '../components/MessengerButton';

storiesOf('Messenger', module)
  .add('default', () => (<Messenger id="olgamawypieki" />));
